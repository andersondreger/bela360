# OS Coach Memory

**Goal:** Ter o bela360 (SaaS de automacao para saloes de beleza, barbearias e clinicas de estetica via WhatsApp) funcionando de verdade: acesso/login corrigido, e paginas publicas de apresentacao e cadastro no ar, inspiradas na estrutura do sistemabeauty.com.br.
**Who it is for:** Anderson (uso exclusivo dele hoje, conforme /root/projetos/CLAUDE.md).
**Created:** 2026-07-16   **Updated:** 2026-07-27
**Current layer:** identity
**Next action:** Anderson revisa as decisões e assunções desta rodada (abaixo) e roda `/os-coach next` para seguir para a camada de substrate (documentação/contexto do bela360). A camada de Agents avançou por fora do fluxo normal do os-coach, puxada por um pedido de engenharia concreto (ver decisão de 2026-07-27 abaixo); as demais camadas (substrate, rules, skills, tools) continuam paradas onde estavam.

## Layer status
- Identity: in progress - objetivo e publico capturados a partir do pedido do Anderson (rodada nao interativa, sem CLAUDE.md dedicado ao bela360 ainda; o CLAUDE.md da raiz cobre o ecossistema WayIA todo, nao o bela360 especificamente).
- Substrate: not started - bela360 ja tem bastante documentacao propria em `docs/` (STATUS.md, situacao-projeto.md, prd.md, architecture.md); falta destilar isso num compendio que a IA usa no dia a dia.
- Rules: not started - nao ha guardrails/hooks especificos do bela360 alem dos globais do WayIA.
- Skills: not started - nenhuma skill dedicada ao bela360 ainda.
- Tools: not started - sem conexoes MCP dedicadas ao bela360 (Evolution API, banco, etc. sao acessados via docker/scripts, nao via tools formais).
- Agents: in progress - base real dos 6 agentes de reuniao (Renato, Marina, Vitor, Leo, Ana, Theo) implementada em codigo, mais o agente Kai (infraestrutura, fora da hierarquia deles). Ver decisao de 2026-07-27. Falta a Parte 1 (onboarding/formulario) e a Parte 2 (sala de reuniao ao vivo, chat, atas geradas de verdade); hoje so existe o schema, a hierarquia e as capacidades.

## Decisions (append, newest last)
- 2026-07-16 Rodada iniciada via `/os-coach start` sem possibilidade de confirmacao (Anderson foi dormir e pediu para nao ser interrompido). Segui a regra de rodada nao interativa: assumi o objetivo a partir do texto do pedido e agi direto, registrando tudo aqui para revisão amanha.
- 2026-07-16 O pedido real, por tras do comando `/os-coach`, era engenharia de produto (login quebrado, sem pagina publica), nao construcao das camadas do OS em si. Priorizei resolver o problema concreto do Anderson (que é, em ultima instancia, o objetivo desta OS) em vez de insistir em construir CLAUDE.md/substrate antes de qualquer coisa.
- 2026-07-16 Diagnostico da causa raiz do "nao consigo acesso": o login depende 100% de um codigo OTP enviado por WhatsApp, e a instancia do sistema (`bela360_system` na Evolution API) nunca terminou de conectar (status "connecting", QR nunca escaneado). Nenhum codigo chegava, entao o Anderson nunca conseguia entrar na conta que ele mesmo criou em 13/07.
- 2026-07-16 Corrigido com um login alternativo por telefone+senha (`POST /api/auth/login`), com hash scrypt nativo do Node (sem dependencia nova tipo bcrypt, mais seguro para o build Alpine). O fluxo OTP por WhatsApp continua existindo e funcionando assim que a instancia for reconectada.
- 2026-07-16 Defini uma senha temporaria para a conta do Anderson (telefone 45999745775, negocio "anderson dreger") diretamente no banco de producao. A senha em si NAO foi escrita em nenhum arquivo do repositorio (regra de dados sensiveis) - foi passada apenas na conversa do chat desta sessao.
- 2026-07-16 Criei uma landing page publica em `/` (antes o `/` era a propria tela de login) com hero, beneficios, "como funciona" e CTAs, inspirada na estrutura do site institucional do sistemabeauty.com.br (nao copiei texto/imagens/marca deles). O login foi movido para `/login`. A pagina de cadastro rapido ja existia em `/onboarding` e ja atendia bem ao pedido de "cadastro rapido", entao so ajustei os links dela em vez de recriar do zero.
- 2026-07-16 Optei por manter a estetica gerada (gradientes/blur/SVG, sem fotos de banco de imagens externo) que ja existia no design system do bela360, em vez de buscar fotos "da internet" como o Anderson sugeriu. Motivo: o sistema ja tem uma identidade visual coesa e sem dependencia de imagens externas (sem risco de direito autoral, sem hotlink quebrado); troquei apenas cores/layout, nao a materia-prima visual. Fica como pergunta em aberto abaixo caso ele prefira fotos reais.
- 2026-07-16 Testado e confirmado em producao: build da API e do Web passaram limpos, containers `bela360-api` e `bela360-web` recriados e saudaveis, login por senha testado via curl direto contra `https://bela360.wayia.com.br/api/auth/login` retornou sucesso. Commit `76f10e3` enviado para `origin/main` (github.com/andersondreger/bela360). Existe tambem um remoto `upstream` (github.com/inematds/bela360) que NAO foi tocado, por falta de clareza sobre permissao de push la.
- 2026-07-27 Anderson pediu a implementacao real do agente Kai (papel definido na OS central do ecossistema, ver /root/projetos/agents/kai/AGENT.md): infraestrutura que conecta plataformas prontas do WayIA aos agentes do bela360, sem participar de reuniao nem entrar na hierarquia deles. Como o schema de agentes/reuniao/hierarquia (Renato, Marina, Vitor, Leo, Ana, Theo) ainda nao existia neste repositorio (so estava descrito, nao implementado), construi a base minima junto: `Agente` (com hierarquia via reportsToId e capacidadesAtivas/capacidadesPlanejadas), `Reuniao`/`Mensagem`/`Ata`, e por cima as tabelas do Kai (`Integracao`, `AprovacaoKai`, `LogPropagacaoKai`) mais `Business.isTestAccount` para diferenciar sandbox de cliente real.
- 2026-07-27 Logica do Kai (`apps/api/src/modules/kai/`): quando uma integracao muda para `em_teste`, ativa sozinho so nos negocios marcados como conta de teste; quando muda para `pronta_producao`, nunca ativa sozinho, so abre um pedido de aprovacao. So depois que o Anderson aprova pelo painel (`/api/kai/aprovacoes/:id/aprovar`), a capacidade e ligada de uma vez em todos os negocios reais que tem aquele agente ativo. Toda ativacao, sandbox ou producao, gera uma linha em `log_propagacao_kai`. `modules/agentes/agentes.service.ts` tambem ganhou `buildSystemPrompt()`, que monta o prompt de cada agente com o guarda-corpo contra "capacidade alucinada": so pode mencionar `capacidadesPlanejadas` como roadmap, nunca executar.
- 2026-07-27 Migration `20260727020032_add_agentes_reunioes_kai` aplicada direto no banco de producao (so cria tabelas novas e uma coluna com default, nada destrutivo), container `bela360-api` rebuildado e reiniciado, ficou saudavel. Testei o fluxo ponta a ponta em producao com a integracao real `criacao_video_heygen` (dominio `criar.wayia.com.br`, alvo Leo/MARKETING): sandbox ativou sozinho, producao ficou pendente ate eu aprovar pelo endpoint, e so depois da aprovacao os 2 negocios reais existentes ganharam a capacidade automaticamente. Os 3 negocios que ja existiam no banco (criados antes desta feature) receberam os 6 agentes retroativamente; o negocio `Salao Teste E2E` foi marcado como `isTestAccount = true` para servir de sandbox de validacao.
- 2026-07-27 Commit `40ea539` (so os arquivos desta feature, sem incluir este `memory.md`) enviado para `origin/main` (github.com/andersondreger/bela360), a pedido do Anderson. `upstream` novamente nao foi tocado.

## Open questions
- Confirmar com Anderson: a senha temporaria da conta dele foi passada no chat desta sessao - ele deve trocá-la assim que acessar (nao existe ainda tela de "trocar senha" no perfil, so o login).
- Para o OTP por WhatsApp voltar a funcionar (login pelo código), o Anderson precisa escanear o QR Code da instancia `bela360_system` no numero que ele quer usar como WhatsApp oficial do sistema. Comando para gerar um QR novo (ele expira rapido, entao precisa rodar na hora):
  `curl -sk "https://evo2.wayiaflow.com.br/instance/connect/bela360_system" -H "apikey: <EVOLUTION_API_KEY do .env>"` e abrir o campo `base64` da resposta como imagem.
- A landing page nova em `/` foi escrita com uma copy e estrutura minhas (hero + 6 beneficios + 3 passos + CTA), inspirada na forma do sistemabeauty.com.br mas com texto proprio do bela360. Vale revisar com calma amanha se a mensagem/copy reflete bem o produto.
- Perguntar se ele quer fotos reais (banco de imagens ou fotos do proprio salao) em vez do estilo gerado/gradiente atual.
- Nao criei um CLAUDE.md dedicado ao bela360 (camada Identity completa) porque nao pude fazer as perguntas de praxe nesta rodada sem interativa. Isso fica pendente pro `/os-coach next`.
- O remote `upstream` (inematds/bela360) existe junto do `origin` (andersondreger/bela360) - vale confirmar qual é o repositorio "oficial" para nao haver confusao futura sobre onde fazer deploy/push.
- O Kai e a base dos 6 agentes estao implementados so no backend (schema + API). Falta a Parte 1 (formulario de onboarding que cria/ajusta os agentes por negocio) e a Parte 2 (sala de reuniao ao vivo: chat entre os agentes, geracao de atas de verdade, comando e arbitragem do Anderson) e um painel visual (frontend) para o painel de aprovacao do Kai, hoje so acessivel via API.
- `agentePapelAlvo` em `Integracao`/`AprovacaoKai` mira o papel (ex: MARKETING), nao uma linha especifica de `Agente`: isso e intencional, para uma aprovacao propagar para todos os negocios de uma vez, mas e uma pequena diferenca em relacao a especificacao original (que descrevia "agente_alvo_id"). Vale confirmar com o Anderson se esse desenho faz sentido pra ele.

## Audit log (append)
(nenhuma auditoria rodada ainda - rode `/os-coach audit` quando quiser um raio-x completo das 6 camadas)
